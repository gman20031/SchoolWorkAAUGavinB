#include "level\LevelData.h"
#include "Entity.h"
#include "Enemy.h"

void Enemy::SetType(char type)
{

}

void Enemy::TurnAround()
{

}

void Enemy::ResetEnemy(levelInfo* currLevel)
{

}

void Enemy::StepEnemy(levelInfo* currLevel, location nextLocation)
{
	//currLevel->at(GetCurrentLocation()) = '.';
	//SetCurrentLocation(nextLocation);
	//currLevel->at(nextLocation) = m_enemyType;
}

		